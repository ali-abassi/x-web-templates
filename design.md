# Design

**Observed:** Alpine Notes, Rainbow Venture, Bloop, and Furion are static HTML/CSS/JavaScript templates with a gallery and standalone downloads.

**Agent-selected working policy:** Preserve existing template appearance, interactions, directory structure, and provenance. Omit the root Sites project identity from the new repository to avoid binding the collection to an existing deployment. Preserve portable template hosting configurations.
