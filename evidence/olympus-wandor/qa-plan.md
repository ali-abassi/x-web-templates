# Frozen scoped QA — two additions

Mode: dogfooding iterate; direct active agent. Environment: real task-owned Chrome 152, Ali Default profile, agent-browser 0.37.1; local static server 4175 for baseline, integrated 4173 then existing personal Pages target. No remote trip service or agent integration is claimed. Sources and documents frozen in plan.json before implementation. Initial source base 855a3b4 clean.

1. Reference fidelity: all source sections, full desktop 1440 and mobile 375, safety 320; compare each still and original proportions; inspect all artwork in layout.
2. Olympus local assignment/unassignment, model selection, workflow run/reset, standards open/close and CTA keyboard route.
3. Wandor empty/whitespace submit, notes -> local draft, edit -> real browser download matching bytes, return preserving notes, three dashboard views, FAQs.
4. Both keyboard focus/feedback/44px primary controls and browser errors; no new runtime exception.
5. Both pause/resume, hidden target activation, reduced motion, rendered motion recording.
6. Both no JavaScript, missing art and doubled labels; preserve readable content and prevent page overflow.
7. All seventeen exports, assets, syntax, zero authored TypeScript, complexity <=5; exact prior fifteen runtime preservation; open both unpacked downloads locally.
8. Clean complete candidate critical self-review, private main push, public Pages bytes and live interactions, gallery navigation/downloads and README screenshots.

Failures are preserved. Only affected cases rerun after each correction. Billing unavailable. No judge scores or independent agent claim.

## Initial findings

OW-01 P1 visual: Olympus serif fell back because a generic family name was used. Baseline/olympus-1440.png shows heavy Times-like titles and a three-line team title. Fix hypothesis: unique OlympusSerif family loads the bundled Instrument face.
OW-02 P1 visual: hands and philosopher head cropped by cover at wide section ratios. Same baseline. Fix hypothesis: top-align hands, contain right-aligned philosopher. Mask square edge on bust/temple with soft CSS alpha.
OW-03 P1 visual: temporary initials and absent classical side figures do not reconstruct the supplied team mosaic. Replace the temporary mosaic with one generated original composite matching that section. This is the first of at most two bounded artwork corrections, following actual rendered inspection; input budget total ceiling remains eleven calls.

OW-04 P1 visual: Wandor preview reused the landmark illustration for every stay, unlike the reference’s Venetian photos. The second and final bounded correction adds one original Venice panorama with distinct CSS thumbnail crops. Baseline/wandor-1440.png retains the issue. Footer tower cropping is corrected by matching display height to the artwork’s actual content band.
OW-05 P1 responsive: Olympus rotating ornamental ellipses produce scrollWidth 414 at width 375. Fix only the decorative layer with a dedicated clipped wrapper; preserve all real content and controls.

QA driver correction: the first security disclosure check clicked successfully (details.open true), but its assertion required contiguous source text across a formatter newline. Preserve the failed result and normalize whitespace in that assertion; no product code changed. Previously passing assignment/model/workflow checks are reused.

QA driver correction: CTA focus was correctly on Research, but the immediate viewport assertion ran during native smooth scrolling. Settled inspection showed top 316 / bottom 360 within the 1000px viewport. Wait for the actual focused target to enter the viewport, then assert. No product code changed; previous passing cases remain reused.

OW-06 P1 accessibility: the scoped axe baseline found low-contrast small accent text (four Wandor nodes, one Olympus node) and one skipped dashboard heading level. Darken only those text colors, use a real dashboard h3, and darken the teal button gradient after manual contrast review. Preserve the hue and composition. All primary navigation targets are at least 44px.

QA driver correction: required textboxes expose [required, ref=…], not [ref=…]. Reference parsing now accepts metadata before the ref. No product action occurred at that failure. All nine Olympus desktop interaction cases had already passed and are skipped on resume.

OW-07 P2 resilience, source review: Wandor cloud animation must require the JS-ready class, so script-free reading never leaves an unpausable decorative animation. The reduced/noJS probes cover that boundary. Interaction summary correction: 38 unique passing cases, with two earlier driver assertion failures retained among 40 rows; the initial driver print counted rows rather than unique final cases.

Final visual review: preserve a literal space before the hero’s desktop-only line break so mobile reads “love. We’ll”. Suppress finite preview entry animation when paused; scrolling a paused page must not leave content frozen partly transparent. Continuous decorative motion still pauses and resumes.

Public verification: the text-locator click on Wandor FAQ reported success without opening the native details element. The failure is retained in live-interactions.json. Focusing the observed #faq summary and pressing Enter opened it in the same public page. The resumed browser check uses explicit keyboard activation and verifies open state. No product change.
