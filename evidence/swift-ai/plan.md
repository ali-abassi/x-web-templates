# Frozen Swift AI run contract

Owner: active agent, host-selected model/effort, no delegates or model judges. Mode: dogfooding iterate; bounded reconstruction, not a SaaS launch. Actor: template visitor evaluating/downloading a reference-led hero. Weakest link: art/composition fidelity.

Owned: templates/swift-ai/**, gallery card/cover, build inventory, root README/intent/design, evidence/swift-ai/**, generated dist/ZIP. All eighteen prior runtime paths preserved by baseline hashes. External delivery only existing private main and personal Cloudflare Pages. No messages, accounts, backend or browser profile changes.

Checks / criterion map: baseline build/check and byte inventory → preservation; source first original and 1440 render → geometry/typography/art comparison;375/320 render → responsive integrity; real browser visible-cue navigation + every CTA + valid/invalid/empty/cancel + keyboard/focus → local behavior; injected missing art and no-JS → honest fallback; browser errors/network/state → privacy; build/check/ZIP parity → standalone distribution; public browser plus all public byte hashes → delivery. Cases SW01 desktop/reference, SW02 mobile375, SW03 narrow320, SW04 navigation dialogs, SW05 form empty-invalid-corrected, SW06 cancel/repeat/keyboard, SW07 noJS, SW08 missing art, SW09 galleryZIP/public. SW07/08 injected automated/mock, others real browser.

Budget: one native ChatGPT illustration and at most one targeted correction (tool billing unavailable; no API billed eval); up to three local correction passes, two self-review passes, two hours soft time boundary. No numeric judge, optimization or calibrated-score claim. One anchor inspected before any further generation. Stop on acceptance, unavailable required gate, repeated infrastructure block, or user steering. Rollback: baseline Git revision and existing deployment retained; never alter prior templates.

Skill checklist frozen: Good Design Standard complete source ledger/thesis/ten levers/three-font comparison/render-adverse-deletion review; GitHub Research literal search/two source teardowns/ranked steal-list and licenses; Imagegen built-in single illustrated asset/spec/invariants/inspection/local persistence/prompt provenance; Dogfooding contract/baseline/UI-first cases/logs/findings/iterations/readiness manifest validator. User direct work overrides independent-agent suggestion; separate critical self-review only.
