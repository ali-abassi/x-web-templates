# Verto dogfood journal
Mode iterate. Owner: active agent, no delegation. Source base 9545657, previous17 unchanged. Exact user post and all source frames retained. Browser is actual Default Chrome; screenshots may hide the unrelated Lindy extension panel in transient DOM only. No private media or messages involved. Native artwork and video limits are frozen in plan.json. Reversible local edits and already-authorized personal Pages delivery; rollback is prior canonical deployment plus Git revert of this scoped change.

Baseline: existing 19-page/17-ZIP check passed before adding Verto to build inventory. Original X post and source frames inspected. Each main user journey starts from visible labels, followed by DOM/network verification. Raw actions, failures, timing and outcomes go in browser-trace.jsonl. Safe brief content only. Preserve all outputs/failures. Stop on scoped pass or a required unavailable boundary, with no blind paid retries.

Motion source attempt 1 rejected: invented white lettering at upper-left throughout all 158 frames; 6.583s video instead of requested6. Motion otherwise turns consistently through front/profile/back. Complete contact sheet inspected. Hypothesis: remove website/typography context from prompt and demand an unmarked color field. One permitted paid correction changes that prompt mechanism, preserving input/seed/model. Encoder will trim to6s per delivery contract instead of relaxing the duration gate. Original failed graph and source retained.

VERTO-F01 P2 visual: initial375px crop cuts goggles/nose at right edge. Hypothesis: shift object-position from61% to75% at mobile breakpoint only. Source media unchanged; desktop unchanged. Recheck full head/hero and real-scroll sections after rebuild.

VERTO-INF01 browser interruption: after Sonnet dialog Escape, eval returned null-element error; tab inventory had only owned t1 about:blank. Reopened the same named/pinned session (no profile substitution or Chrome restart), explicitly repeated Sonnet focus/Enter/Escape, verified URL remains/verto and dialog.open=false. Cause of unexpected blank target is unconfirmed; not a product failure or evidence of a crash. Original seven passed checks and raw trace retained. Restarting browser checks under the restored target.

## VERTO-F02 — unseekable video retried continuously
Real local Chrome reproduction: Python SimpleHTTP returned 200 to byte-range requests, buffered [0,6] but seekable [0,0]. Assigning currentTime=2 immediately reported 0 and fired seeked, so the unconditional seeked listener kept retrying an impossible target. Browser interaction later timed out/reset; the exact reset mechanism remains unproven. Paused motion passed the same dialog cycles. Fix: clamp desired time to the actual seekable end, return zero when no seekable range exists, and recheck on progress. Same non-range origin now stays responsive at scrollY=2000 with no active seek. serve 14.2.4 on 4174 returns 206 and seekable [0,6]; scrollY=2000 seeks to 2.590899. Full affected interaction rerun follows. No extra paid calls. Evidence: final/non-range-recovery.json, final/range-scroll.json and browser-trace.jsonl.

## VERTO-F04 — deployed Pages has no media seek range
Starting state: first 18-template deployment ba81c615-9502-4c24-9fd7-f709ff051405. All 208 public hashes matched; actual public scroll check failed because seekable was [0,0] and currentTime stayed 0. Server answered Range with HTTP 200. Guard prevented hangs but did not fulfill motion. Hypothesis: a fetched Blob gives Chrome a complete local seekable source without new hosting. Implemented one HTTP loader using fetch/response.ok/blob/object URL and preserved file-origin direct loading. Local same non-range server proof: source=blob, scrollY=2000, time=2.618261, range end=6. Full desktop/mobile 53-check rerun passed. Reduced/no-JS/fetch failure/hidden checks rerun; public correction pending. Previous failure retained at final/public-before-blob-failure.json.

Corrected public candidate a0160fde-0123-4dad-8ba3-625e15e96c11: 208/208 file hashes matched. Gallery, actual ZIP download, live Blob seek, service selection, project details/Escape and actual mobile brief export passed (8 checks). A public-check helper had a JavaScript selector quoting SyntaxError after service selection; corrected only the helper and resumed remaining checks from the observed state. Raw error retained in browser-trace.jsonl; no failed check was relabelled as a product pass. Final loader's file-origin branch also rechecked from the newly extracted ZIP.
