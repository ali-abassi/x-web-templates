{
  "name": "x-web-templates",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "python3 scripts/build.py",
    "dev": "npm run build && python3 -m http.server 4173 --bind 127.0.0.1 --directory dist",
    "check": "python3 scripts/check.py"
  }
}
